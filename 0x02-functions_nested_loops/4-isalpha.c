#include "main.h"
 /**
  * _isalpha checks for an alphabet
  * returns 1 if it's a letter whether uppercase or lowercase
  * returns 0 otherwise
  * @c character is the one to check
  *
  */

int _isalpha(int c)
{
	if ((c >= 65 && c <= 90) || (c >= 97 && c <= 122)
	{
	return (1);
	}	
	return (0);
}

