#include "main.h"

/**
 * -mul - multiplies two integers.
 * @a: is the first character.
 * @b: is the second character.
 *
 *  Return: result.
 */

int mul(int a, int b)
{
	int result = a * b;
	return (result);
}
